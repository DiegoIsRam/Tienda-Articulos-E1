package com.mx.ids.articulos.security.enums;

public enum RolNombre {
	// El aplicativo tiene dos roles:
	// admin(Tiene todos los permisos CRUD) y user(solo ver los productos)
	ROLE_ADMIN, ROLE_USER
}
